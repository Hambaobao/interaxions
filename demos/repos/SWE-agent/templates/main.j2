#!/bin/bash

echo "=== SWE-Agent Rollout Started ==="

# Read instance data from artifact (mounted by Argo at /tmp/instance_data/)
INSTANCE_DATA_FILE=/tmp/instance_data/data.json
if [ ! -f "$INSTANCE_DATA_FILE" ]; then
    echo "ERROR: instance_data artifact not found at $INSTANCE_DATA_FILE"
    exit 1
fi

INSTANCE_ID=$(cat $INSTANCE_DATA_FILE | jq -rM .instance_id)
DATASET=$(cat $INSTANCE_DATA_FILE | jq -rM .dataset)
SPLIT=$(cat $INSTANCE_DATA_FILE | jq -rM .split)

echo "Instance ID: $INSTANCE_ID"
echo "Dataset: $DATASET"
echo "Split: $SPLIT"

# Model config (rendered at build time)
echo "Model: {{ model }}"
echo "Provider: {{ provider }}"
echo "Max Iterations: {{ max_iterations }}"
echo "SWE-Agent Config: {{ sweagent_config }}"

mkdir -p $OUTPUT_DIR

echo "Waiting for SWE-ReX service at http://localhost:10101 ..."
while true; do
    if curl -f -s -H "X-API-Key: $INSTANCE_ID" --max-time 2 "http://localhost:10101/is_alive" > /dev/null 2>&1; then
        echo "SWE-ReX service is available!"
        break
    fi
    sleep 1
done

echo "=== Preparing Problem Statement ==="
cp /tmp/shared/preprocess/* $OUTPUT_DIR/
CONFIG_DICT_PATH=$OUTPUT_DIR/config_dict.json
echo "Problem statement prepared"

echo "=== Running SWE-Agent ==="
if [ -f "$CONFIG_DICT_PATH" ]; then
    language="$(cat $CONFIG_DICT_PATH | jq -rM .language)"
    language_specific_step="$(cat $CONFIG_DICT_PATH | jq -rM .language_specific_step)"
    extra_fields=$(jq -c -n --arg language "$language" --arg language_specific_step "$language_specific_step" '{"language":$language,"language_specific_step":$language_specific_step}')
else
    extra_fields='{}'
fi

{% set completion = {} %}
{% if max_tokens %}
  {% set _ = completion.update({"max_tokens": max_tokens}) %}
{% endif %}

conda run -n swe-agent sweagent run \
    --output_dir $OUTPUT_DIR \
    --problem_statement.id=$INSTANCE_ID \
    --problem_statement.type=text_file \
    --problem_statement.path=$OUTPUT_DIR/problem_statement.md \
    --problem_statement.extra_fields="$extra_fields" \
    --env.repo.type=preexisting \
    --env.repo.repo_name=$(cat $CONFIG_DICT_PATH | jq -rM .workdir) \
    --env.repo.base_commit=$(cat $CONFIG_DICT_PATH | jq -rM .base_commit) \
    --env.deployment.type=remote \
    --env.deployment.host=localhost \
    --env.deployment.port=10101 \
    --env.deployment.auth_token=$INSTANCE_ID \
    --env.deployment.timeout=300 \
    --agent.model.name={{ provider }}/{{ model }} \
    --agent.model.api_base={{ base_url }} \
    --agent.model.api_key={{ api_key }} \
    --agent.model.temperature={{ temperature }} \
{%- if top_p is not none %}
    --agent.model.top_p={{ top_p }} \
{%- endif %}
    --agent.model.completion_kwargs='{{ completion | tojson }}' \
    --agent.model.per_instance_call_limit={{ max_iterations }} \
    --agent.model.retry.retries={{ num_retries }} \
    --agent.tools.parse_function.type={{ tools_parse_function }} \
    --agent.templates.max_observation_length={{ max_observation_length }} \
    --config config/{{ sweagent_config }}.yaml

echo "SWE-Agent execution completed"

if [ -f "$OUTPUT_DIR/$INSTANCE_ID/$INSTANCE_ID.pred" ]; then
    cp $OUTPUT_DIR/$INSTANCE_ID/$INSTANCE_ID.pred $OUTPUT_DIR/output.sweb.jsonl
    echo "Predictions copied to output.sweb.jsonl"
fi
